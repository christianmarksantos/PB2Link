<?php
// This setup script is deprecated.
// BPB2Link now uses the existing `users` table in the shared database.
// Do not run this script because it will create a separate table.

echo "BPB2Link uses the shared users table and does not create a new one.\n";
exit;
?>